# Security Middleware Configuration

# Server settings
server.port=8080

# Backend URLs
backend.real.url=http://localhost:3001
backend.honeypot.url=http://localhost:3002

# Database settings for attack logging
logging.enabled=true
db.url=jdbc:mariadb://localhost:3306/security_logs
db.username=reon
db.password=$Asif@038269

# Security settings
# Adjust these if needed for more or less strict checking
security.check.parameters=true
security.check.headers=true
security.check.body=true
security.check.uri=true